const SYSTEM=`Eres Rubén Coach, asistente deportivo educativo de Sumando Minutos. Responde en español, con tono cálido y conciso. Ayuda con deportes, actividades, técnica básica, videos y objetivos progresivos. No diagnostiques ni sustituyas atención médica; ante dolor, lesión o riesgo recomienda detenerse y consultar a un profesional. No inventes datos personales ni afirmes haber visto actividades que no recibiste.`;
exports.handler=async event=>{
  if(event.httpMethod!=='POST')return {statusCode:405,body:JSON.stringify({error:'Método no permitido'})};
  if(!process.env.OPENAI_API_KEY)return {statusCode:503,body:JSON.stringify({error:'Rubén Coach aún no está configurado'})};
  try{const body=JSON.parse(event.body||'{}');const message=String(body.message||'').trim().slice(0,1200);if(!message)return {statusCode:400,body:JSON.stringify({error:'Mensaje vacío'})};const response=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'content-type':'application/json'},body:JSON.stringify({model:process.env.OPENAI_MODEL||'gpt-5-mini',instructions:SYSTEM,input:message,max_output_tokens:350})});const data=await response.json();if(!response.ok)throw new Error();const text=data.output_text||data.output?.flatMap(x=>x.content||[]).find(x=>x.type==='output_text')?.text||'No pude responder en este momento.';return {statusCode:200,headers:{'content-type':'application/json','cache-control':'no-store'},body:JSON.stringify({reply:text})};}catch(error){return {statusCode:500,headers:{'content-type':'application/json'},body:JSON.stringify({error:'No fue posible consultar a Rubén Coach'})};}
};
